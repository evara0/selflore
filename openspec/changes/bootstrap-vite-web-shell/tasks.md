## 1. 项目初始化

- [ ] 1.1 在 `apps/web/` 创建 Vite React TypeScript pnpm 项目，并验证 `pnpm install` 与 `pnpm build` 成功
- [ ] 1.2 配置开发服务器端口 `24567`，并验证启动后页面可访问

## 2. 布局框架

- [ ] 2.1 创建自定义 Lattice / SELFLORE 标识和多项顶部导航，并通过浏览器检查布局
- [ ] 2.2 用 CSS 创建线条网格背景和半透明内容面板，并验证背景在面板周围与透过面板均可见
- [ ] 2.3 实现导航状态切换与空白/模拟内容面板，并验证每项导航可切换

## 3. 验证

- [ ] 3.1 运行 `pnpm build` 并检查仅包含布局所需前端文件与默认单主题
