## 1. uv 项目与依赖

- [x] 1.1 在 `apps/api/` 初始化 Python 3.13 的 uv 项目，创建隔离 `.venv/`，并验证 `.venv/` 受根 `.gitignore` 忽略而 `pyproject.toml` 可跟踪
- [x] 1.2 通过 uv 添加运行依赖 FastAPI 与 Uvicorn、测试依赖 pytest 与 httpx，生成 `uv.lock`，并用 `uv sync --frozen` 验证锁定依赖可从空环境同步

## 2. 最小健康检查服务

- [x] 2.1 创建最小 FastAPI 应用模块，只实现 `GET /api/health` 并返回 `200` 与 `{"ok": true}`，通过应用内测试客户端验证完整状态码和响应体
- [x] 2.2 添加健康检查测试，并用 `uv run pytest` 验证在没有数据库、认证配置或环境秘密时测试通过
- [x] 2.3 添加 API 运行说明，提供绑定 `127.0.0.1:24566` 的 `uv run` 启动命令，并验证说明不引入其他端点或业务配置

## 3. 端到端验证

- [x] 3.1 使用项目启动命令运行服务，向 `http://127.0.0.1:24566/api/health` 发出请求并验证获得 `200` 与 `{"ok": true}`，随后停止测试进程
- [x] 3.2 审查 `apps/api/` 文件清单，确认只有健康检查所需的项目、应用、测试和说明文件，且没有数据库、认证、秘密或其他业务端点
